version: '3.8'

services:
  nse-scraper:
    build:
      context: .
    container_name: nse-scraper

    # Mount downloads directory to persist CSV files on host
    volumes:
      - ./downloads:/app/downloads

    # Share host network (optional, helps with some networking scenarios)
    # network_mode: host

    # Set environment variables
    environment:
      - DISPLAY=:99
      - PYTHONUNBUFFERED=1

    # Shared memory size (important for Chrome stability)
    shm_size: 1gb

    # Command to run (override with docker-compose run arguments)
    # Example: docker-compose run nse-scraper TATASTEEL --headless
    command: ["--help"]

    # Security options
    security_opt:
      - seccomp:unconfined

    # Resource limits (optional)
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 500M

  nse-scraper-server:
    build:
      context: .
      dockerfile: Dockerfile.server
    container_name: nse-scraper-server

    # Mount downloads directory to persist CSV files on host
    volumes:
      - ./downloads:/app/downloads
